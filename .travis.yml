env:
    - USER=travis

sudo: false

language: go

go:
  - 1.9

before_script:
  - make vet
  - go tool vet post-processor/vsphere-template/step_mark_as_template.go

install:
  - make deps

script:
  - GOMAXPROCS=2 make ci

branches:
  only:
    - master

matrix:
  fast_finish: true
